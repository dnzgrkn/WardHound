__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
!.env.example
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
node_modules/
dist/
build/
.next/
*.log
.DS_Store
.idea/
.vscode/
docker-compose.override.yml

# Agent instruction/prompt files — kept locally so Claude Code/Codex still
# read them each session, but not published in the public repo.
CLAUDE.md
AGENTS.md
prompts/
ml/data/raw/

# Real lab data snapshots pulled by the scripts/ingest_*.py bridge scripts —
# contain real internship identifiers (usernames, hostnames, MACs), never
# for the public repo. See project confidentiality rule.
ad_events.json
pf_events.json
